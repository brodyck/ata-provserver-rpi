#!/bin/bash

serverDir="/home/pi/ata-provserver"
startupScript="ata-provserver"

cp -f ${serverDir}/${startupScript} /etc/init.d/${startupScript}

update-rc.d ata-provserver defaults
/etc/init.d/ata-provserver start
